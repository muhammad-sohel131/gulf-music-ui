import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Gulf Coast Music - Live Performances, Festivals & Local Artists",
  description: "Discover the vibrant Gulf Coast music scene! Explore live performances, festivals, local artists, and cultural events along the Gulf Coast. Stay updated on concerts, venues, and the best music experiences in the region.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
